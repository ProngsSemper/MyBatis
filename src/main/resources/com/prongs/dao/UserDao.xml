<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.prongs.dao.UserDao">
    <!--配置查询所有-->
    <select id="findAll" resultType="com.prongs.domain.User">
        select * from user
    </select>

    <!--保存用户-->
    <insert id="saveUser" parameterType="com.prongs.domain.User">
        <!--配置插入后操作，获取新用户id-->
        <selectKey keyProperty="id" keyColumn="id" resultType="int" order="AFTER">
        select last_insert_id();
        </selectKey>
        insert into user (username,address,sex,birthday)values (#{username},#{address},#{sex},#{birthday})
    </insert>

    <!--更新用户-->
    <update id="updateUser" parameterType="com.prongs.domain.User">
        update  user  set username=#{username},address=#{address},sex=#{sex},birthday=#{birthday} where id=#{id}
    </update>

    <!--删除用户-->
    <delete id="deleteUser" parameterType="java.lang.Integer">
        delete from user where id=#{uid}
    </delete>

    <!--根据id查找用户-->
    <select id="findUserById" parameterType="java.lang.Integer" resultType="com.prongs.domain.User">
        select * from user where id=#{uid}
    </select>

    <!--根据name查找用户-->
    <select id="findUserByName" parameterType="java.lang.String" resultType="com.prongs.domain.User">
        select * from user where username like '%${value}%'
    </select>

    <!--查询总用户数-->
    <select id="total" resultType="java.lang.Integer">
        select count(id) from user
    </select>

    <!--根据QueryVo查找用户-->
    <select id="findUserByVo" parameterType="com.prongs.domain.QueryVo" resultType="com.prongs.domain.User">
        select * from user where username like #{user.username}
    </select>
</mapper>